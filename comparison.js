// Make sure while doing comparisons make variables of same data types otherwise you will get unpredictable results.

// === strict check -> checks for both datatype and the value

// console.log("2"===2);  the result would be false as it does no conversion and strictly checks them!!
